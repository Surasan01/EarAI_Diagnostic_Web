# ============================================================================
# .dockerignore – Exclude unnecessary files from the Docker build context
# ============================================================================

# Node modules & build artifacts
node_modules
dist
build
.next
out

# Backend code (not needed in the frontend image)
convex/

# Environment files – keep secrets out of the image
.env*
.env

# Version control & IDE metadata
.git
.gitignore
.vscode
.idea
.DS_Store

# Logs, coverage, and caches
*.log
coverage
.cache

# Docker configuration files
Dockerfile
docker-compose.yml
docker-compose.override.yml